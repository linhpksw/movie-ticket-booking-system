@inherits LayoutComponentBase

<!-- Header (pull out to a separate component if desired) -->
<HeaderComponent />

<!-- Main content rendered here -->
<div class="main-content">
    @Body
</div>

<!-- Footer (pull out to a separate component if desired) -->
<FooterComponent />